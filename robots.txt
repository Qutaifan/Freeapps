# Google AdSense Crawler
User-agent: Mediapartners-Google
Allow: /

# Google Search Engine Crawler
User-agent: *
Allow: /

# Repository / tooling files. These are served only because the deploy
# publishes the repository root; they are not site content and should not
# be indexed.
Disallow: /AGENTS.md
Disallow: /*/AGENTS.md
Disallow: /CLAUDE.md
Disallow: /README.md
Disallow: /MY-NOTES/
Disallow: /freeapps-components/
Disallow: /scripts/
Disallow: /.vs/
# Internal remediation specs, agent reports and fact sheets. Deployed only
# because the deploy publishes the repository root. No user value, and the
# superseded prompts describe the site's own quality failures.
Disallow: /docs/
# Dead Next.js route payload (see AGENTS.md §8). Referenced by nothing the
# static pages load. search-index.json is deliberately NOT blocked - search.js
# fetches it at runtime.
Disallow: /index.txt

# Sitemap (single file; URL count is generated, see sitemap.xml)
Sitemap: https://www.qutaifan.com/sitemap.xml
