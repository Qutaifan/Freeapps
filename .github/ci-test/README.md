# Temporary CI test scaffolding (v2)

This directory exists only inside the disposable `ci/temp-pr-branch-v2`
branch, used to open a throwaway PR that exercises the v2 `content-quality`
and `compliance` GitHub Actions checks. Never intended to be merged; will
be deleted along with the branch once verification is complete.
